The mobilenet Algorithm should be in the main file so as to work !

The library collection enables us to have the last x recent values of a list
It does an update of values from the last value being newest and does a shift to the left 
and it maintains the size of the list constant.

Conclusion : it is sweatable when we need a list with a constant size ,doing constant updates